apiVersion: v1
kind: Service
metadata:
  name: {{ include "dhana-lakshmi.fullname" . }}
  labels:
    {{- include "dhana-lakshmi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dhana-lakshmi.selectorLabels" . | nindent 4 }}
